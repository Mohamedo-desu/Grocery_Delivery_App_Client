import { Dimensions } from "react-native";

export const screenWidth: number = Dimensions.get("window").width;
export const screenHeight: number = Dimensions.get("window").height;

export const NoticeHeight = screenHeight * 0.7;
